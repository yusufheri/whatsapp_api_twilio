{% extends 'base.html.twig' %}

{% block title %}WhatsApp API - Login{% endblock %}

{% block body %}
<div class="container">
            {% set _hasErrors = "" %}
            {% set _username = "" %}
      <div class="forms-container">
        <div class="signin-signup">
          {% if hasErrors is defined %}
            {% set _hasErrors = hasErrors %}
            {% set _username = username %}
          {% endif %}

          {% include "account/login.html.twig" with {hasErrors: _hasErrors, username: _username} %}

          {% include "account/register.html.twig" %}
        </div>
      </div>

      <div class="panels-container">
        <div class="panel left-panel">
          <div class="content">
            <h3>Etes-vous nouveau ?</h3>
            <p>
              Créer votre compte dans moins de cinq minutes et patienter que l'administrateur active votre compte
               (moins de 24h)
            </p>
            <button class="btn transparent" id="sign-up-btn">
              <i class="fa fa-user-plus" aria-hidden="true"></i> Inscription
            </button>
          </div>
          <img src="img/log.svg" class="image" alt="" />
        </div>
        <div class="panel right-panel">
          <div class="content">
            <h3>Avez-vous déjà un compte ?</h3>
            <p>
              Connectez-vous et accédez à votre espace réservé pour envoyer vos messages à vos contacts
              <i class="fa fa-smile-o" aria-hidden="true"></i>&nbsp;
              <i class="fa fa-smile-o" aria-hidden="true"></i>&nbsp;
              <i class="fa fa-smile-o" aria-hidden="true"></i>
            </p>
            <button class="btn transparent" id="sign-in-btn">
              <i class="fa fa-lock" aria-hidden="true"></i> Connexion
            </button>
          </div>
          <img src="img/register.svg" class="image" alt="" />
        </div>
      </div>
    </div>

    <script src="app.js"></script>
{% endblock %}
